import { DefaultTemplate } from '../components/templates/DefaultTemplate'

export const Home = () => {
    return (
        <DefaultTemplate>
            <h1>Usando Context</h1>
        </DefaultTemplate>
    )
}