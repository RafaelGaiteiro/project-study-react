import { styled } from "styled-components";

export const TopicComponent = styled.div`
  padding: 8px;
`;

export const Title = styled.h1`
  font-size: 18px;
  font-weight: 500;
  color: white;
  user-select: none;
`;